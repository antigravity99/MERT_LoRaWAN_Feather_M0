namespace MERT
{
    public enum State
    {
        Active,
        Inactive,
        Error
    }

    public enum DeviceUSBStatus
    {
        Connected,
        Connecting,
        Disconnected,
        Error
    }
}